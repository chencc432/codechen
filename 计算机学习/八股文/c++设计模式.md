单例模式就是一个类只能创建一个对象，这个对象为各个任务进行服务，创建的方式是在类中设置静态函数，不能用new，因为new后会马上释放

饿汉模式：定义类时，单例对象就已经创建了，更适合多线程情况
懒汉模式：什么时候使用这个单例模式，什么时候创建，这样就比较节省空间 ，有线程安全问题，多个线程会创建多个对象，需要加互斥锁，会更麻烦，饿汉模式没这个问题，可以用双重检查锁定去优化效率，也可以用静态局部对象去解决

static里面的new分为三条机器指令，多线程时还是可能会乱序，引入c++中的原子变量，使得指令按序执行（懒汉的多线程中使用）